name: suc-so-rancher
targetCustomizations:
- name: local
  clusterSelector:
    matchLabels:
      management.cattle.io/cluster-display-name: local
  yaml:
    suc:
    # Refere-se ao diretório overlay
    - suc-so
